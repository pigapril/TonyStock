/**
 * AdminOnly Component Export
 * 
 * Provides clean imports for the AdminOnly component and related utilities.
 */

import AdminOnly from './AdminOnly';
import './AdminOnly.css';

export default AdminOnly;
export { AdminOnly };