// Intentionally empty.
//
// We provide our own Chart.js date adapter override in `homeChartRegistry`.
// Aliasing `chartjs-adapter-date-fns` to this shim prevents stale or implicit
// side-effect imports from pulling in a second adapter runtime.
